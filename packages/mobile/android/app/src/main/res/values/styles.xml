<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="AppTheme" parent="Theme.EdgeToEdge">
        <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">true</item>
        <item name="enforceNavigationBarContrast">false</item>
    </style>
    <!-- Base application theme. -->
    <style name="BootTheme" parent="Theme.BootSplash">
        <item name="bootSplashBackground">@color/bootsplash_background</item>
        <item name="bootSplashLogo">@drawable/bootsplash_logo</item>
        <!-- <item name="bootSplashBrand">@drawable/bootsplash_brand</item> -->
        <item name="postBootSplashTheme">@style/AppTheme</item>
        <!--
        3 things to know to use windowLightStatusBar / windowLightNavigationBar:
        - true = dark-content, false = light-content barStyle
        - windowLightStatusBar should target at least "m" api
        - windowLightNavigationBar should target at least "o_mr1" api

        For this example, we are using a transparent status bar + an opaque navigation bar.
        For that, we only need to set the status color as the bootsplash background color.
        -->
        <item name="android:statusBarColor">@color/bootsplash_background</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

</resources>
